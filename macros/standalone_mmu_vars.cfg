# REMOVED 2026-08-22 — this file and macros/standalone_mmu_hooks.cfg defined the
# Happy-Hare-shaped "_STANDALONE_*" hook scaffolding (_STANDALONE_PRE_LOAD, _STANDALONE_POST_
# UNLOAD, etc.) built specifically to wire BLOBIFIER/BLOBIFIER_PARK into the load/unload flow.
# Rolled back along with that Blobifier wiring at user's request — hardware/blobifier.cfg was
# moved out to ~/blobifier.cfg and macros/filament.cfg no longer calls any of these hooks.
# Left empty here on purpose — do not add content without reconstructing why this was removed.
