# Moved to late_includes/standalone_mmu_client.cfg 2026-08-20 — this file's PAUSE/RESUME/
# CANCEL_PRINT rename_existing wrapper needs to load AFTER mainsail.cfg, but living inside
# macros/ meant the [include macros/*.cfg] wildcard also picked it up (Klipper dedupes
# [include] by resolved path, so it only actually loaded once — at the wildcard's earlier
# position, before mainsail.cfg even ran). See late_includes/standalone_mmu_client.cfg and
# printer.cfg's include comment for the fix. Left empty here on purpose — do not add content.
